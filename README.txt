First, change the DefaultConnection in appsettings.json:

"DefaultConnection": "Server=host;Database=SportOrders;TrustServerCertificate=true;Integrated Security=true;MultipleActiveResultSets=true"

Change "host" for your server or datasource.


After, run the SQL File "Algartech.SqlScript.sql"

After, run the Project